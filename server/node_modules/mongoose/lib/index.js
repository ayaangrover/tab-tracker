'use strict';

/*!
 * Module dependencies.
 */

const mongodbDriver = require('mongoose/lib/drivers/node-mongodb-native');

require('mongoose/lib/driver').set(mongodbDriver);

const mongoose = require('mongoose/lib/mongoose');

mongoose.setDriver(mongodbDriver);

mongoose.Mongoose.prototype.mongo = require('mongodb');

module.exports = mongoose;
